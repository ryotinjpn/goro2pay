# Cognito module: User Pool + App Client + Pre Sign-up Lambda Trigger
# unit-of-work.md §4.1 の `infra/modules/cognito/` 定義に準拠。
# Auth Unit が所有する論理リソース群。
#
# resource 定義は機能別ファイル (cognito.tf / pre_signup_lambda.tf / iam.tf) に配置。
